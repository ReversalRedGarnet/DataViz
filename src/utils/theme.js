// Real color values for places JS/D3 needs an actual string (Tailwind
// classes don't work inside D3's .attr('fill', ...) calls). Keep these
// in sync with the ocean/sun tokens in tailwind.config.js.
export const SELECTION_COLORS = ['#5B8FA3', '#F0C868'] // pick 1 (ocean), pick 2 (sun)
